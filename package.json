{
  "name": "projekte",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy:pages": "npx wrangler pages deploy dist/ --project-name=projekte",
    "deploy:workers": "npx wrangler deploy"
  }
}
